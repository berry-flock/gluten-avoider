<%- include("partials/header") %>

<section class="section-block">
  <div class="section-heading section-heading--roomy">
    <div>
      <p class="eyebrow">Map</p>
      <h1><span class="module-icon module-icon--sky"></span>Browse saved places on the map</h1>
    </div>
    <a class="button button--secondary" href="/places">Browse and search</a>
  </div>
  <div id="map-view" class="map-view-canvas"></div>
</section>

<section class="section-block">
  <div class="section-heading section-heading--roomy">
    <div>
      <h2><%= mappablePlaces.length %> matching place<%= mappablePlaces.length === 1 ? "" : "s" %></h2>
      <p class="section-copy"><%= placesWithoutCoordinates %> saved place<%= placesWithoutCoordinates === 1 ? "" : "s" %> are not shown because they do not have coordinates yet.</p>
    </div>
  </div>

  <% if (mappablePlaces.length) { %>
    <div class="card-grid">
      <% mappablePlaces.forEach((place) => { %>
        <%- include("partials/place-card", { place }) %>
      <% }) %>
    </div>
  <% } else { %>
    <p class="empty-state">No mapped places are available right now.</p>
  <% } %>
</section>

<script
  src="https://unpkg.com/leaflet@1.9.4/dist/leaflet.js"
  integrity="sha256-20nQCchB9co0qIjJZRGuk2/Z9VM+kNiyxNV1lvTlZBo="
  crossorigin=""
></script>
<script id="map-view-data" type="application/json"><%- JSON.stringify({
  places: mappablePlaces.map((place) => ({
    lat: Number(place.latitude),
    lng: Number(place.longitude),
    name: place.name,
    slug: place.slug,
    suburb: place.suburb
  }))
}) %></script>
<script src="/map-view.js"></script>

<%- include("partials/footer") %>
