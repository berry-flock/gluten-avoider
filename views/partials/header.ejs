<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= pageTitle %> | Gluten Avoider</title>
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=Orbitron:wght@500;700;800&family=Nunito:wght@500;600;700;800&display=swap" rel="stylesheet" />
    <link rel="stylesheet" href="/styles.css" />
    <%- typeof extraHead !== "undefined" ? extraHead : "" %>
  </head>
  <body>
    <header class="site-header">
      <div class="shell site-header__inner">
        <a class="site-title" href="/">Gluten Avoider</a>
        <nav class="site-nav">
          <a class="site-nav__link" href="/nearby"><span class="module-icon module-icon--pink"></span><span>Nearby</span></a>
          <a class="site-nav__link" href="/plan"><span class="module-icon module-icon--yellow"></span><span>Plan</span></a>
          <a class="site-nav__link" href="<%= currentAdmin ? "/admin" : "/admin/login" %>"><span class="module-icon module-icon--lavender"></span><span><%= currentAdmin ? "Admin" : "Login" %></span></a>
        </nav>
      </div>
    </header>
    <main class="shell page-content">
